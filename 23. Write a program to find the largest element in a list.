void main() {
  List<int> numbers = [10, 25, 7, 40, 15];
  int largest = numbers[0];
  for (int num in numbers) {
    if (num > largest) {
      largest = num;
    }
  }
  print("Largest = $largest");
}
